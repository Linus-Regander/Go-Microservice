version: "3.9"

services:
  service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: go-microservice
    ports:
      - "8080:8080"
    environment:
      SERVICE_PORT: "8080"
    env_file:
      - .env